import { Command, flags } from '@oclif/command';
export default class Deploy extends Command {
    static description: string;
    static flags: {
        hash: flags.IOptionFlag<string | undefined>;
    };
    private readonly controller;
    run(): Promise<void>;
}
