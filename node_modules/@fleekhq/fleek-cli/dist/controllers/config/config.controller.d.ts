import { DatabaseService } from '../../services/database/database.service';
import { EnvironmentType } from '../../services/environment/enums/environment-type';
import { ShowConfigResult } from './models';
export declare class ConfigController {
    private databaseService;
    constructor(databaseService: DatabaseService);
    /**
     * Returns the config from the db
     */
    showConfig(): Promise<ShowConfigResult>;
    /**
     * Updates the environment config
     * @param environment The environment
     */
    updateEnvironmentConfig(environment: EnvironmentType): void;
}
export default ConfigController;
