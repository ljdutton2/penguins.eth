import { ApiService } from '../../services/api/api.service';
import { IpfsService } from '../../services/ipfs/ipfs.service';
import { EnvironmentService } from '../../services/environment/environment.service';
export declare class SiteController {
    private apiService;
    private ipfsService;
    private environmentService;
    constructor(apiService: ApiService, ipfsService: IpfsService, environmentService: EnvironmentService);
    init(): Promise<void>;
    private updateConfigWithSiteInfo;
    private createSite;
    private static mapBuildSettings;
    private publishIpfsSourceFiles;
    /**
     * Trigger deploying of the current site.
     */
    deploy(commitHash?: string): Promise<void>;
    private getDeploymentUrl;
}
export default SiteController;
