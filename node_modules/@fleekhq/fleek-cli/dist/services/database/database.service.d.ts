import { EnvironmentType } from '../environment/enums/environment-type';
export interface ConfigurationModel {
    /**
     * Determines which Fleek server we should use.
     */
    environment: EnvironmentType;
    /**
     * Result of the initial login result.
     */
    session: SessionInformation | undefined;
    /**
     * Map each teamId to its information.
     */
    teams: Record<string, TeamInfo | undefined>;
}
export interface SessionInformation {
    username: string;
    apiKey: string;
    apiSecret: string;
}
export interface TeamInfo {
    name: string;
    apiKey: string;
}
export declare class DatabaseService {
    private conf;
    get<T extends keyof ConfigurationModel>(key: T): ConfigurationModel[T];
    set<T extends keyof ConfigurationModel>(key: T, value: ConfigurationModel[T] | undefined): void;
}
