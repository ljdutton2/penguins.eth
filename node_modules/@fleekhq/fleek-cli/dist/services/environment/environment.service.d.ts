import { DatabaseService } from '../database/database.service';
import { EnvironmentType } from './enums/environment-type';
export declare class EnvironmentService {
    private databaseService;
    private environmentToEndpointMappings;
    private authEndpointMappings;
    private appUrlMappings;
    private authRedirectUrlMappings;
    private appsyncApiUrlMappings;
    private publicApiUrlMappings;
    private fleekIpfsUrlMappings;
    constructor(databaseService: DatabaseService);
    /**
     * Get the current environment url
     */
    getCurrentEnvironmentUrl(): Promise<string>;
    /**
     * Get the current auth environment url
     */
    getCurrentEnvAuthUrl(): Promise<string>;
    /**
     * Get the current redirect_uri environment url for auth
     */
    getCurrentEnvAuthRedirectUrl(): Promise<string>;
    /**
     * Get the appsync api url
     */
    getCurrentAppsyncUrl(): Promise<string>;
    /**
     * Get the current public api url.
     */
    getCurrentApiUrl(): Promise<string>;
    /**
     * Get Fleek Web App Url
     */
    getCurrentAppUrl(): Promise<string>;
    getCurrentIpfsUrl(): Promise<string>;
}
export default EnvironmentType;
